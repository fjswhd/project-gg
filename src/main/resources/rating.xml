<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ratingns">
	<select id="selectRatingCount" resultType="int">
		select nvl(max(r_no),0)+1 from rating
	</select>
	<insert id="insertRscore" parameterType="rating">
		insert into rating values( #{r_no},#{m_id},#{b_no},#{r_score})	
	</insert>
</mapper> 